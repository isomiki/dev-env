# Remove development environment setup

Use `docker-compose.yml` to make a Docker container.

Inside the container, create a setup script at `/scripts/setup.sh` with the contents of the `setup.sh` script in this repo.
